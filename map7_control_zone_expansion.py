"""Superseded duplicate of map7_control_panel_comparison.py, kept only so
older links resolve. Produces the identical figure from model_results.json."""
from map7_control_panel_comparison import draw

if __name__ == "__main__":
    draw("outputs/plots/control_zone_expansion.png")
